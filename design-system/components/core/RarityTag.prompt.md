Loot-tier label shown on equipment cards next to the item name.

```jsx
<RarityTag rarity="epic" />
```

Tiers: `common` (muted grey), `rare` (INT blue), `epic` (FOC violet). Labels are Spanish (Común / Raro / Épico) to match the product voice.
